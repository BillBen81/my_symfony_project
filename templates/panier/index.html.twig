{% extends 'base.html.twig' %}

{% block title %}Hello PanierController!{% endblock %}

{% block body %}
    <h1>Panier  | <a href="{{ path('app_liste_produit') }}"> Liste des produits</a></h1>

    <table class="table">
        <thead>
        <tr>
            <th>Description</th>
            <th>Prix</th>
            <th>Quantité</th>
            <th>Total</th>
        </tr>
        </thead>
        <tbody>
        {% set i = -1 %}
        {% for panier in titemPanier %}
            {% set i = i + 1 %}
            <tr>
                <td>{{ panier.description }}</td>
                <td>{{ panier.prix }}</td>
                <td>{{ tnbItem[i] }}</td>
              <!--  <td>{{ panier.prix * tnbItem[i]}}</td> -->
                <td>{{ tiPrixAchat[i]}}
            </tr>

        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        <tr>
            <td colspan="3" style="font-weight: bold">Total achat</td>
            <td>{{  iSum }}</td>
        </tr>
        </tbody>
    </table>


{% endblock %}
