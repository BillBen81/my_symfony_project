{% extends 'base.html.twig' %}

{% block title %}Entreprises{% endblock %}

{% block body %}
    <h1>Entreprises</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ entreprise.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entreprise.email }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ entreprise.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ entreprise.adresse }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ entreprise.createAt ? entreprise.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ entreprise.UpdateAt ? entreprise.UpdateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entreprises_index') }}">Retour à la liste</a>

    <a href="{{ path('app_entreprises_edit', {'id': entreprise.id}) }}">Editer</a>

    {{ include('entreprises/_delete_form.html.twig') }}
{% endblock %}
